/**
 * Maps conceptual service keys to Sanity `serviceCategory` values for related posts.
 * City service pages use `holiday-lighting` → `christmas-lights` in CMS.
 */
export const SERVICE_SLUG_TO_CATEGORY: Record<string, string> = {
  "holiday-lighting": "christmas-lights",
};
